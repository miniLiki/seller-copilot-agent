services:
  db:
    image: postgres:16
    environment:
      POSTGRES_DB: seller_copilot
      POSTGRES_USER: seller
      POSTGRES_PASSWORD: seller
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U seller -d seller_copilot"]
      interval: 5s
      timeout: 5s
      retries: 10

  api:
    build: .
    environment:
      SELLER_COPILOT_STORAGE: mock
      DATABASE_URL: postgresql+psycopg://seller:seller@db:5432/seller_copilot
      CORS_ORIGINS: http://localhost:5173,http://127.0.0.1:5173
    ports:
      - "8000:8000"
    depends_on:
      db:
        condition: service_healthy

  web:
    image: node:22-slim
    working_dir: /app
    command: sh -c "npm install && npm run dev"
    environment:
      VITE_API_BASE_URL: http://localhost:8000
    ports:
      - "5173:5173"
    volumes:
      - ./web:/app
    depends_on:
      - api

volumes:
  postgres_data:

# Seller Copilot Agent 需求文档

## 1. 项目目标

构建一个可演示、可训练、可评测的多模态电商运营 Agent，面向跨境电商场景完成以下闭环：

1. 用户输入商品主图和任务指令。
2. 系统对图片进行诊断。
3. 系统检索业务规则与 SOP。
4. 模型基于图像和规则给出结构化结论。
5. 若需要执行动作，系统自动调用 FastAPI 工具。
6. 系统返回最终执行结果与可解释日志。

项目名称：`seller-copilot-agent`

目标定位：面试项目，不追求生产级复杂度，优先保证可运行、可展示、结构清晰、代码完整。

---

## 2. MVP 场景

只实现一个核心业务闭环：

### 场景：商品主图诊断与执行

输入：

* 一张商品主图
* 用户自然语言任务，例如：

  * `请分析这张商品主图是否适合美国站投放，如不适合请直接创建整改任务并生成两条广告文案。商品ID 是 SKU_001。`

输出：

* 图像诊断结论
* 证据列表
* 检索到的规则片段
* 工具调用记录
* 工具返回结果
* 最终回复

执行动作只支持：

* 创建素材整改工单
* 生成广告文案

不做：

* 用户系统
* 权限系统
* 数据库持久化
* 真实 ERP/CRM 对接
* 前端管理后台

---

## 3. 技术栈

### 必须使用

* Python 3.10+
* FastAPI
* Uvicorn
* requests
* pydantic
* ms-swift

### 可选使用

* typer 或 argparse：CLI
* jinja2：Prompt 模板
* rank-bm25 或简单关键词匹配：检索
* pytest：单元测试

### 不要求

* 前端框架
* Docker
* Redis
* PostgreSQL
* Celery

---

## 4. 系统模块

系统拆成 6 个模块：

1. `tools/`：模拟 ERP/CRM 的 FastAPI 工具服务
2. `rag/`：本地规则库检索
3. `agent/`：任务编排、Prompt 构造、模型输出解析、工具执行
4. `train/`：ms-swift 训练和推理脚本
5. `demo/`：CLI 演示入口
6. `eval/`：最小评测脚本

---

## 5. 项目目录结构

```text
seller-copilot-agent/
├─ README.md
├─ requirements.txt
├─ .env.example
├─ data/
│  ├─ train.jsonl
│  ├─ val.jsonl
│  ├─ images/
│  │  ├─ sku_001_main.jpg
│  │  └─ sku_002_main.jpg
│  └─ kb/
│     ├─ sop_ad_creative.md
│     ├─ sop_title_rules.md
│     ├─ sop_detail_page.md
│     └─ inventory_policy.md
├─ tools/
│  ├─ __init__.py
│  ├─ app.py
│  ├─ schemas.py
│  └─ mock_db.py
├─ rag/
│  ├─ __init__.py
│  ├─ kb_loader.py
│  ├─ retrieve.py
│  └─ chunking.py
├─ agent/
│  ├─ __init__.py
│  ├─ prompt.py
│  ├─ planner.py
│  ├─ executor.py
│  ├─ parser.py
│  ├─ models.py
│  └─ tool_registry.py
├─ train/
│  ├─ sft_lora.sh
│  ├─ infer.sh
│  └─ merge_lora.sh
├─ demo/
│  ├─ __init__.py
│  └─ cli_demo.py
├─ eval/
│  ├─ __init__.py
│  ├─ eval_tool_call.py
│  ├─ eval_diag.py
│  └─ eval_e2e.py
└─ tests/
   ├─ test_tools.py
   ├─ test_retriever.py
   └─ test_parser.py
```



## 5.1 新增总原则

1. **训练协议与运行协议分离**：

   * 训练协议遵循 ms-swift 官方 Agent / multimodal dataset format；
   * 运行协议使用项目自定义 JSON 输出，便于 demo、日志和评测。

2. **工具 schema 单一来源**：

   * `agent/tool_registry.py` 作为所有工具定义的唯一来源；
   * FastAPI request/response model、Prompt 中的工具定义、训练数据生成脚本中的 tools 字段，都从这里派生。

3. **默认 mock 可运行，真实模型可插拔**：

   * 所有代码必须在无 GPU、无真实模型时仍可跑通 demo；
   * 真实 ms-swift 推理通过适配层接入，不破坏 mock 模式。

4. **所有关键步骤可追踪**：

   * 检索结果、Prompt 摘要、模型原始输出、解析后的 tool_calls、工具响应、最终回答都要能落盘。

5. **仓库对 AI coding assistant 友好**：

   * 增加面向 Codex 的仓库内说明文件，减少自动生成歧义。

---

## 6. 功能需求

### 6.1 工具服务模块 `tools/`

实现一个本地 FastAPI 服务，默认端口 `8000`。

#### API 1：获取商品信息

* 方法：`GET /product/{product_id}`
* 作用：返回商品基础信息
* 返回字段：

  * `product_id`
  * `title`
  * `market`
  * `category`
  * `price`
  * `inventory`
  * `hero_image_url`
  * `status`

#### API 2：获取库存状态

* 方法：`GET /inventory/{product_id}`
* 作用：返回库存与风险等级
* 返回字段：

  * `product_id`
  * `inventory`
  * `safety_stock`
  * `risk_level`，枚举值：`low | medium | high`

#### API 3：创建优化工单

* 方法：`POST /task/create`
* 请求字段：

  * `product_id`
  * `task_type`，枚举值：`creative_refresh | detail_page_fix | title_rewrite`
  * `priority`，枚举值：`low | medium | high`
  * `reason`
* 返回字段：

  * `task_id`
  * `product_id`
  * `task_type`
  * `priority`
  * `status`

#### API 4：生成广告文案

* 方法：`POST /copy/generate`
* 请求字段：

  * `product_id`
  * `market`
  * `angle`，枚举值：`benefit_driven | pain_point | discount`
  * `num_variants`，1 到 5
* 返回字段：

  * `product_id`
  * `copies`，字符串数组

#### API 实现要求

* 使用 Pydantic 定义请求和响应模型。
* 数据源使用内存字典 mock。
* 若商品不存在，返回 404。
* 返回 JSON 结构必须稳定，不允许字段名漂移。

---

### 6.2 规则检索模块 `rag/`

新增要求：

* 检索结果必须保存 `source`、`chunk_id`、`score`、`content`。
* 检索模块必须支持 deterministic 模式，便于测试。
* 需要提供一个 `dump_retrieval_trace()` 方法，把当前检索结果保存到 `runs/` 目录。
* chunk 切分必须稳定，不允许每次运行 chunk_id 变化。

### 6.2 规则检索模块 `rag/`

目标：从 `data/kb/` 中检索与当前任务最相关的规则片段。

#### 输入

* 用户任务文本
* 可选：商品类别、市场信息

#### 输出

* Top-3 规则片段，每条包含：

  * `source`
  * `content`
  * `score`

#### 实现要求

* 第一版使用本地文件 + 简单检索即可。
* 支持 markdown 文件加载。
* 允许使用：

  * 简单关键词匹配
  * BM25
* 不强制向量数据库。

#### 规则切分要求

* 每个 markdown 文件按段落切分。
* 保留 `source` 字段。
* 检索结果按分数降序返回。

---

### 6.3 Agent 模块 `agent/`

这是核心模块，负责把图像、用户任务、RAG 结果、工具 schema 拼成 prompt，并执行整条链路。

#### 输入

* `image_path`
* `user_query`
* `product_id`

#### 执行流程

1. 读取用户输入。
2. 检索规则片段。
3. 组装 prompt。
4. 调用模型，获取结构化 JSON 输出。
5. 解析 `tool_calls`。
6. 调用 FastAPI 工具。
7. 汇总结果。
8. 输出最终日志。

#### 必须实现的组件

新增目录：

* `runs/`：保存每次 demo 或评测的 trace 日志
* `scripts/`：放置数据生成和校验脚本
* `docs/`：放置额外设计说明与示意图

新增文件：

* `AGENTS.md`：给 Codex / Claude Code / Cursor 的仓库操作说明
* `scripts/generate_seed_data.py`：自动生成 20+ 条种子训练样本
* `scripts/validate_dataset.py`：校验 `train.jsonl` / `val.jsonl` 是否满足 ms-swift 格式与本项目约束
* `scripts/generate_placeholder_images.py`：生成占位示例图，保证仓库 clone 后可直接运行

#### 必须实现的组件

##### `models.py`

负责定义运行时协议与结构化类型。

要求：

* 使用 Pydantic 定义以下模型：

  * `ToolCall`
  * `ModelRuntimeOutput`
  * `ToolExecutionRecord`
  * `RunTrace`
* 这些模型作为 parser、executor、planner 之间的公共契约。

##### `prompt.py`

负责构造系统提示词与用户提示词。

要求：

* Prompt 中必须包含：

  * 角色定义
  * 检索到的规则片段
  * 可用工具 schema
  * 用户任务
  * 输出 JSON 格式要求
* Prompt 需区分两种模板：

  * `runtime_json_prompt`：用于 demo / runtime，要求输出统一 JSON
  * `swift_agent_prompt`：用于与 ms-swift Agent 数据协议保持概念一致
* Prompt 构造必须支持插入 `<image>` 标签占位；当传入多模态样本时，`<image>` 数量必须与 `images` 长度一致。这与 ms-swift 的多模态 Agent 数据要求一致。([github.com](https://github.com/modelscope/ms-swift/blob/main/docs/source_en/Instruction/Agent-support.md))

##### `parser.py`

负责解析模型输出。

要求：

* 尝试从字符串中解析 JSON。
* 若解析失败，返回结构化错误。
* 支持字段校验。

##### `tool_registry.py`

负责维护工具定义与调用映射。

要求：

* 这是工具 schema 的唯一来源。
* 提供以下导出函数：

  * `get_tool_schemas_for_prompt()`
  * `get_tool_schemas_for_training()`
  * `get_tool_executor_map()`
* 训练数据生成脚本必须直接复用这里的 schema，禁止手写重复 schema。

##### `tool_registry.py`

负责维护工具定义与调用映射。

要求：

* 定义所有工具 schema
* 把工具名映射到 HTTP 请求实现

##### `executor.py`

负责执行工具调用。

要求：

* 顺序执行多个 `tool_calls`
* 收集每次调用的输入、输出、错误
* 任意单个工具失败时不中断主流程，写入错误日志

##### `planner.py`

作为高层 orchestrator，串起检索、prompt、模型调用、工具执行、最终输出。

---

## 7. 训练协议与运行协议

### 7.1 训练协议（新增）

训练数据默认遵循 ms-swift 官方 Agent 数据格式，而不是只在 assistant 中输出手写 JSON。

推荐训练样本结构：

```json
{
  "tools": "[{\"type\": \"function\", \"function\": {\"name\": \"create_opt_task\", \"description\": \"Create an optimization task\", \"parameters\": {\"type\": \"object\", \"properties\": {\"product_id\": {\"type\": \"string\"}, \"task_type\": {\"type\": \"string\"}, \"priority\": {\"type\": \"string\"}, \"reason\": {\"type\": \"string\"}}, \"required\": [\"product_id\", \"task_type\", \"priority\", \"reason\"]}}}]",
  "images": ["data/images/sku_001_main.jpg"],
  "messages": [
    {"role": "user", "content": "<image> 请分析这张商品主图是否适合美国站投放，如不适合请创建整改任务。"},
    {"role": "tool_call", "content": "{\"name\": \"create_opt_task\", \"arguments\": {\"product_id\": \"SKU_001\", \"task_type\": \"creative_refresh\", \"priority\": \"high\", \"reason\": \"hero image is weak for paid ads\"}}"},
    {"role": "tool_response", "content": "{\"task_id\": \"TASK_001\", \"status\": \"created\"}"},
    {"role": "assistant", "content": "已创建素材整改工单，建议进一步生成两条美国站广告文案。"}
  ]
}
```

要求：

* 多模态 Agent 样本中，`<image>` 标签数量必须与 `images` 字段长度一致。([github.com](https://github.com/modelscope/ms-swift/blob/main/docs/source_en/Instruction/Agent-support.md))
* 工具调用训练样本优先使用 `tool_call` 与 `tool_response` 角色，而不是仅靠 assistant 自由文本模拟。ms-swift 官方 Agent Support 文档支持这一格式，并会通过 `agent_template` 自动映射。([github.com](https://github.com/modelscope/ms-swift/blob/main/docs/source_en/Instruction/Agent-support.md))
* 如需要兼容简单 SFT，可保留部分 assistant-only 样本，但比例不应超过 30%。

### 7.2 运行协议

运行时，模型或 mock model 最终都要被适配成项目统一的 JSON 输出，供 CLI demo 和评测使用。

## 7.3 模型输出协议

模型必须严格输出 JSON，禁止输出自由文本。

JSON 结构如下：

```json
{
  "task_understanding": "string",
  "evidence": ["string", "string"],
  "need_rag": true,
  "need_tool_call": true,
  "tool_calls": [
    {
      "name": "create_opt_task",
      "arguments": {
        "product_id": "SKU_001",
        "task_type": "creative_refresh",
        "priority": "high",
        "reason": "hero image is weak for paid ads"
      }
    },
    {
      "name": "generate_ad_copy",
      "arguments": {
        "product_id": "SKU_001",
        "market": "US",
        "angle": "benefit_driven",
        "num_variants": 2
      }
    }
  ],
  "final_response": "已创建整改工单，并生成两条广告文案。"
}
```

约束：

* `evidence` 必须是字符串数组。
* `tool_calls` 可为空数组。
* 工具名必须来自预定义工具列表。
* 所有参数字段必须符合 schema。

---

## 8. 演示模式

### 8.1 假模型模式

先实现一个 `mock_model()`，返回固定 JSON，用于验证全链路。

要求：

* 不依赖真实模型。
* 可以直接根据 query 中是否出现“创建工单”“生成文案”等关键词构造结果。
* 该模式用于快速打通 demo。

### 8.2 真实模型模式

保留一个模型调用接口，例如：

* `call_model(prompt: str, image_path: str) -> str`

要求：

* 该函数独立封装。
* 后续可以替换为 ms-swift 推理。

---

## 9. CLI 演示入口 `demo/cli_demo.py`

实现一个命令行脚本，支持如下调用：

```bash
python -m demo.cli_demo \
  --image data/images/sku_001_main.jpg \
  --product-id SKU_001 \
  --query "请分析这张商品主图是否适合美国站投放，如不适合请直接创建整改任务并生成两条广告文案。"
```

输出要求：

* 终端打印以下模块化日志：

  * User Query
  * Retrieved Rules
  * Model Output
  * Tool Calls
  * Tool Results
  * Final Response

要求输出整洁，适合面试演示。

---

## 9.1 模板与推理一致性要求（新增）

需要新增一个 `configs/runtime.yaml` 或等价配置文件，至少包含：

* `model_mode`
* `model_name`
* `adapter_path`
* `agent_template`
* `tools_prompt`
* `seed`
* `tool_base_url`
* `save_run_trace`

要求：

* 训练和推理都显式记录 `agent_template` 与 `tools_prompt`。
* 默认推荐在脚本中暴露这两个配置项，避免模板不一致。
* 记录随机种子与运行时间戳，便于复现。ms-swift 官方命令行参数文档中也明确提供了 `adapters`、`seed`、`external_plugins` 等关键参数。([github.com](https://github.com/modelscope/ms-swift/blob/main/docs/source_en/Instruction/Command-line-parameters.md))

## 10. 数据需求

### 10.1 知识库文件

在 `data/kb/` 下至少提供 4 个 markdown 文件：

1. `sop_ad_creative.md`
2. `sop_title_rules.md`
3. `sop_detail_page.md`
4. `inventory_policy.md`

每个文件包含 3 到 8 条规则。

### 10.2 训练数据

在 `data/train.jsonl` 和 `data/val.jsonl` 中提供示例数据。

必须包含三类样本：

#### A. 图像诊断样本

字段：

* `messages`
* `images`

#### B. 官方 Agent 格式工具调用样本

字段：

* `messages`
* `tools`
* 可选：`images`

要求：

* 至少一半以上的工具调用数据使用 `tool_call` / `tool_response` 角色。
* 至少 5 条样本包含 `<image>` + `images` 的多模态 Agent 格式。

#### C. 运行时 JSON 协议样本

字段：

* `messages`
* 可选：`images`

作用：

* 用于帮助模型在 runtime 模式下更稳定地产生项目规定的 JSON 输出。

#### B. 工具调用样本

字段：

* `messages`
* `tools`

建议最小数量：

* train：20 条
* val：5 条

要求 Codex 自动生成至少 20 条高质量种子样本，内容围绕以下主题：

* 主图主体不突出
* 卖点不明确
* 绝对化用语违规
* 促销信息不一致
* 广告图背景干扰大
* 需要创建整改工单
* 需要生成广告文案

---

## 11. ms-swift 训练需求

### 11.1 训练脚本 `train/sft_lora.sh`

生成一个 bash 脚本用于 LoRA 微调。

默认命令骨架：

```bash
swift sft \
  --model Qwen/Qwen2.5-VL-7B-Instruct \
  --train_type lora \
  --dataset data/train.jsonl \
  --val_dataset data/val.jsonl \
  --split_dataset_ratio 0.0 \
  --torch_dtype bfloat16 \
  --num_train_epochs 3 \
  --per_device_train_batch_size 1 \
  --gradient_accumulation_steps 8 \
  --learning_rate 1e-4 \
  --lora_rank 8 \
  --lora_alpha 32 \
  --target_modules all-linear \
  --max_length 4096 \
  --remove_unused_columns false \
  --output_dir output/seller_copilot_lora
```

要求：

* 脚本有 `set -eux`
* 可直接执行
* 可替换 model 名称

### 11.2 推理脚本 `train/infer.sh`

要求生成 ms-swift 推理脚本，支持 adapter 加载。

### 11.3 合并脚本 `train/merge_lora.sh`

要求生成 LoRA merge 脚本。

---

## 11.1 数据质量与校验（新增）

必须新增一个 `scripts/validate_dataset.py`，实现以下检查：

* 每条样本必须包含 `messages`
* 若包含 `images`，则文件路径必须存在
* 若 `messages` 中出现 `<image>`，其数量必须与 `images` 长度一致
* 若存在 `tools`，必须是合法 JSON 或 Python 可解析对象
* 若使用 `tool_call` / `tool_response`，角色顺序必须合法
* 输出校验报告，指出错误样本编号

该要求是为了和 ms-swift 的标准 / Agent 数据格式保持一致。([swift.readthedocs.io](https://swift.readthedocs.io/en/latest/Customization/Custom-dataset.html))

## 12. 配置与环境变量

需要 `.env.example`，至少包含：

```env
TOOL_BASE_URL=http://127.0.0.1:8000
MODEL_MODE=mock
MODEL_NAME=Qwen/Qwen2.5-VL-7B-Instruct
ADAPTER_PATH=
```

要求：

* 代码中通过环境变量读取工具服务地址和模型模式。
* 模型模式至少支持：

  * `mock`
  * `swift`
* 还需支持 `save_run_trace=true/false`。
* 推荐补充：

  * `AGENT_TEMPLATE`
  * `TOOLS_PROMPT`
  * `SEED`
  * `RUN_DIR`

---

## 12.1 可观测性与 trace 要求（新增）

参考成熟 Agent 模板项目和 Agentic Multimodal RAG 项目的做法，需增加最小可观测性。([github.com](https://github.com/vstorm-co/full-stack-ai-agent-template))

必须实现：

* 每次 demo / eval 运行都在 `runs/<timestamp>_<case_id>/` 下保存：

  * `input.json`
  * `retrieval.json`
  * `prompt.txt`
  * `raw_model_output.txt`
  * `parsed_output.json`
  * `tool_results.json`
  * `final_response.txt`
* `planner.py` 返回一个 `RunTrace` 对象。
* CLI demo 提供 `--save-trace/--no-save-trace`。

这是面试演示的重要加分项。

---

## 13. 错误处理需求

### 工具调用错误

* 若工具返回非 200，记录错误信息。
* 主流程继续执行。
* 最终响应中包含失败说明。

### 模型输出错误

* 若 JSON 解析失败，返回统一结构：

```json
{
  "error": "MODEL_OUTPUT_PARSE_ERROR",
  "raw_output": "..."
}
```

### 文件缺失错误

* 图片不存在时抛出清晰异常。
* KB 目录为空时给出可读报错。

---

## 13.1 契约测试（新增）

需要新增 `tests/test_contracts.py`，测试以下内容：

* `tool_registry.py` 导出的 schema 与 FastAPI request model 字段一致
* prompt 中展示的工具 schema 与 registry 一致
* 数据生成脚本生成的 `tools` 字段与 registry 一致

目的：避免 schema 漂移。

## 14. 单元测试需求

至少写 3 组测试：

1. `test_tools.py`

* 测试商品查询接口
* 测试创建工单接口
* 测试广告文案生成接口

2. `test_retriever.py`

* 测试 KB 能正常加载
* 测试检索结果数量和排序

3. `test_parser.py`

* 测试合法 JSON 输出解析
* 测试非法 JSON 输出报错

要求：

* 使用 pytest
* 能直接运行 `pytest -q`

---

## 15. 评测需求

实现 3 个最小评测脚本：

### `eval_tool_call.py`

指标：

* tool name accuracy
* argument exact match

### `eval_diag.py`

指标：

* evidence item hit rate
* final action correctness

### `eval_e2e.py`

指标：

* end-to-end success rate

评测数据可以使用小型手工样本。

---

## 15.1 给 AI coding assistant 的仓库内说明（新增）

新增文件 `AGENTS.md`，内容至少包含：

* 项目目标
* 目录说明
* mock 模式优先
* 代码修改原则
* 禁止破坏 CLI demo
* 工具 schema 单一来源在 `agent/tool_registry.py`
* 训练协议与运行协议分离

这是参考 AI-agent-friendly 模板项目补充的要求。([github.com](https://github.com/vstorm-co/full-stack-ai-agent-template))

## 16. README 要求

README 必须包含以下部分：

1. 项目简介
2. 系统架构图（ASCII 即可）
3. 功能列表
4. 目录结构
5. 环境安装
6. 启动工具服务
7. 运行 CLI Demo
8. 训练脚本使用方法
9. 评测脚本使用方法
10. 项目亮点

README 风格要求：

* 适合面试展示
* 简洁但完整
* 命令都可复制

---

## 17. 代码风格要求

* 所有 Python 代码必须带类型标注。
* 模块职责清晰，避免一个文件过大。
* 关键函数必须写 docstring。
* 不要过度抽象。
* 以可读性优先。

---

## 17.1 复现性要求（新增）

需要新增以下约束：

* 所有脚本默认固定随机种子
* 训练脚本与 demo 脚本都打印关键配置
* `runs/` 目录下保存本次运行配置快照
* `train/` 下增加一个 `README.md`，解释如何替换模型与 adapter

## 18. 交付要求

Codex 最终应生成一个完整可运行项目，满足：

1. `uvicorn tools.app:app --reload --port 8000` 可启动工具服务。
2. `python -m demo.cli_demo ...` 可跑通假模型模式。
3. 有完整的训练数据样例。
4. 有 ms-swift 训练/推理/合并脚本。
5. 有最小测试与评测脚本。
6. 仓库结构清晰，README 完整。

---

## 19. 开发优先级

Codex 按以下顺序生成代码：

### P0

* `tools/`
* `rag/`
* `agent/` 中 mock 模型模式
* `demo/cli_demo.py`
* `README.md`
* `requirements.txt`

### P1

* `data/` 下训练样本与 KB
* `train/` 下脚本
* `tests/`

### P2

* `eval/`
* 真实 ms-swift 推理接口占位实现

---

## 20. 给 Codex 的直接指令

请基于本需求文档直接生成完整项目代码，不要只给伪代码。要求：

1. 先生成完整目录结构。
2. 每个文件都给出完整内容。
3. 默认保证 mock 模式可运行。
4. 所有 import 路径正确。
5. 代码能在本地直接启动。
6. 优先保证演示链路稳定，而不是追求复杂功能。
7. 如真实 ms-swift 推理难以在无模型环境下运行，保留接口并默认使用 mock 模式。
8. 生成的数据样例和 README 必须与代码保持一致。

---

## 21. 非目标

以下内容本项目不做：

* 前端网页 UI
* 用户鉴权
* 多租户
* 真实数据库
* 云部署
* 分布式训练
* 复杂多 Agent 协作
* 真实 ERP/CRM 集成

---

## 21.1 建议补充的非必须实现（新增）

以下能力不是当前 MVP 必须项，但如果 Codex 能顺手补上，可显著提升项目质量：

* 自动从 `tool_registry.py` 生成 OpenAPI 风格工具文档
* 增加 `submit_ab_test` 工具
* 增加 `mock_model` 与 `swift_model_adapter` 的统一接口抽象
* 增加 `Makefile`，提供 `make demo` / `make test` / `make validate-data`

## 22. 验收标准

若满足以下条件，则项目视为完成：

1. 本地工具服务能启动。
2. CLI demo 能看到检索、工具调用和最终回执。
3. 代码目录完整、命名清晰。
4. README 可指导他人复现。
5. 训练数据与脚本已准备好。
6. 测试和评测脚本存在且结构合理。
7. 至少存在 1 条符合 ms-swift 官方 Agent 格式的多模态工具调用训练样本。
8. 运行一次 CLI demo 后，`runs/` 目录下能看到完整 trace。
9. `scripts/validate_dataset.py` 能通过现有训练集。
10. `AGENTS.md` 存在且内容与实际代码结构一致。

---

## 23. 建议的后续扩展

如果 Codex 还有余力，可进一步扩展：

* 增加 Streamlit demo
* 增加更多工具，如 `submit_ab_test`
* 增加库存预警场景
* 增加更多训练样本模板
* 增加更强的 RAG 检索器

以上扩展都不是当前必须项。
